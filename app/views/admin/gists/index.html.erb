<div class="row justify-content-center mt-5">
  <div class="col col-md-6">
    <h2 class="text-center"> Gists list</h2>
      <%= link_to 'Back', admin_tests_path %>
    <table class="table text-center">
      <thead class="thead" style="background-color: lightgray";>
        <tr>
          <th><%= t('.question')%></th>
          <th><%= t('.gist_link')%></th>
          <th><%= t('.user_email') %></th>
        </tr>
      </thead>
     <%= render @gists %>
    </table>
  </div>
</div>
